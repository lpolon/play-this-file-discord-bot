import {sendEmbed} from './controller'



export default {
  summon: {
    joinChannel(message) {
      if (message.member.voice.channel) {
        
      }
      return connection
    },
    feedbackMsg: ``
  },
};
